# 持股匯入/匯出功能使用指南

## 功能概述

本系統提供完整的持股資料備份與還原功能，支援在不同裝置間同步持股資料。

## 新增功能

### 1. 選擇匯出路徑（支援新版瀏覽器）

#### 支援的瀏覽器
- Chrome/Edge 86+
- Safari 未支援（會降級為傳統下載）
- Firefox 未支援（會降級為傳統下載）

#### 使用方式

**在持股列表頁面：**
1. 點擊「📥 匯出」按鈕
2. 系統會彈出檔案儲存對話框
3. 選擇儲存位置和檔案名稱
4. 點擊「儲存」

**在設定頁面：**
1. 進入「⚙️ 設定」頁面
2. 在「💾 持股資料備份」區域點擊「📥 匯出持股」
3. 選擇儲存位置

#### 降級處理
如果瀏覽器不支援檔案選擇對話框，系統會自動使用傳統下載方式：
- 檔案會下載到瀏覽器預設下載資料夾
- 檔案名稱格式：`持股備份_YYYY-MM-DD.json`

### 2. 記錄上次匯入資訊

系統會自動記錄最近一次匯入的資訊：
- **檔案名稱**：匯入的 JSON 檔案名稱
- **匯入時間**：完整的匯入日期時間
- **匯入數量**：成功匯入的持股筆數

#### 查看匯入記錄

1. 進入「⚙️ 設定」頁面
2. 在「💾 持股資料備份」區域下方
3. 會顯示「上次匯入記錄」資訊卡片

範例：
```
上次匯入記錄：
檔案：持股備份_2026-05-18.json
時間：2026/05/18 下午2:30:15
數量：12 筆
```

### 3. 持股列表頁面新增匯入/匯出按鈕

現在可以直接在持股列表頁面進行匯入/匯出操作：

**位置**：📊 持股 → 📋 持股列表 tab

**按鈕**：
- 🔄 重新載入
- 📥 匯出
- 📤 匯入
- 🗑️ 清空全部

**優勢**：
- 無需切換到設定頁面
- 匯入後立即顯示最新持股
- 操作更直覺快速

## 匯出功能詳細說明

### 匯出資料格式

匯出的 JSON 檔案包含以下欄位：

```json
[
  {
    "symbol": "2330.TW",
    "name": "台積電",
    "shares": 100,
    "avg_price": 580,
    "note": "核心持股",
    "holding_bucket": "stable_profit"
  }
]
```

### 匯出流程

1. **發送 API 請求**：
   ```
   GET /api/portfolio/export
   Headers: X-User-ID: <your_user_id>
   ```

2. **選擇儲存位置**（新功能）：
   - 支援瀏覽器會顯示檔案選擇對話框
   - 可自訂檔案名稱和儲存路徑

3. **確認匯出**：
   - 顯示匯出成功訊息
   - 包含匯出的持股數量

## 匯入功能詳細說明

### 支援的檔案格式

- **檔案類型**：JSON (.json)
- **編碼**：UTF-8
- **結構**：陣列格式，包含持股物件

### 必要欄位

- `symbol`：股票代碼（必填）

### 選填欄位

- `name`：股票名稱
- `shares`：持股數量
- `avg_price`：平均成本
- `note`：備註
- `holding_bucket`：持股分類（`short_term` 或 `stable_profit`）

### 匯入流程

1. **選擇檔案**：
   - 點擊「📤 匯入」按鈕
   - 選擇之前匯出的 JSON 檔案

2. **自動處理**：
   - 系統會清空現有持股（預設行為）
   - 批量匯入新資料
   - 驗證資料格式

3. **顯示結果**：
   - 成功數量
   - 失敗數量（如果有）
   - 來源檔案名稱

4. **記錄資訊**：
   - 自動儲存匯入記錄到本機
   - 可在設定頁面查看

### 錯誤處理

系統會檢查以下錯誤：

1. **檔案格式錯誤**：
   ```
   ❌ 檔案格式錯誤，應為持股陣列
   ```

2. **API 錯誤**：
   ```
   ❌ 匯入 API 失敗：<具體錯誤訊息>
   ```

3. **部分失敗**：
   ```
   ✅ 匯入完成：10 筆成功，2 筆失敗
   ```

## 使用場景

### 場景 1：備份持股資料

定期匯出持股資料作為備份：

1. 每週或每月匯出一次
2. 使用日期命名，例如：`持股備份_2026-05-18.json`
3. 儲存到雲端硬碟或外接硬碟

### 場景 2：跨裝置同步

在手機和電腦間同步持股：

1. **手機端**：
   - 匯出持股資料
   - 透過雲端服務（Google Drive、iCloud 等）同步

2. **電腦端**：
   - 下載同步的檔案
   - 匯入到電腦版應用

3. **設定相同 User ID**：
   - 在兩端設定相同的 LINE User ID
   - 可直接共用伺服器端資料（無需手動匯入/匯出）

### 場景 3：資料遷移

更換裝置或重新安裝時：

1. **舊裝置**：
   - 匯出完整持股資料

2. **新裝置**：
   - 安裝應用
   - 匯入之前匯出的資料
   - 設定相同的 User ID（如果需要）

### 場景 4：測試或實驗

在不影響實際持股的情況下測試功能：

1. 先匯出目前持股（作為備份）
2. 進行各種測試操作
3. 如需還原，直接匯入備份檔案

## 進階技巧

### 手動編輯匯出檔案

匯出的 JSON 檔案可以手動編輯：

1. **批量修改**：
   - 使用文字編輯器開啟 JSON 檔案
   - 批量修改股數、成本等資訊
   - 儲存後重新匯入

2. **合併多個備份**：
   - 將多個 JSON 檔案的陣列內容合併
   - 確保 JSON 格式正確
   - 匯入合併後的檔案

3. **篩選特定持股**：
   - 刪除不需要的持股物件
   - 只保留特定分類的持股

### 自動化備份（進階）

可以定期手動匯出，或使用瀏覽器自動化工具：

```javascript
// 瀏覽器控制台執行（範例）
document.getElementById('btnExportHoldings').click();
```

## 注意事項

1. **資料覆蓋**：
   - 匯入預設會**清空現有持股**
   - 匯入前請確認是否需要備份目前資料

2. **User ID 一致性**：
   - 匯入/匯出與 User ID 綁定
   - 不同裝置若要共用資料，需設定相同 User ID

3. **本機快取**：
   - 系統會自動快取最新持股到本機
   - 離線時可讀取快取資料
   - 匯入後會更新快取

4. **檔案安全**：
   - JSON 檔案以明文儲存
   - 建議不要包含敏感個人資訊
   - 備份檔案請妥善保管

5. **瀏覽器相容性**：
   - 檔案選擇對話框需要較新版本瀏覽器
   - 舊版瀏覽器會自動降級為傳統下載方式
   - 功能不受影響，僅使用體驗略有差異

## 疑難排解

### Q: 匯出按鈕沒有反應？

A: 檢查以下項目：
1. 確認已載入持股資料
2. 檢查瀏覽器控制台是否有錯誤訊息
3. 確認網路連線正常

### Q: 匯入後資料不見了？

A: 可能原因：
1. 檔案格式錯誤 → 檢查錯誤訊息
2. API 請求失敗 → 重試或檢查網路
3. User ID 不同 → 確認登入的 User ID

### Q: 如何在不同裝置間同步？

A: 兩種方式：
1. **手動同步**：使用匯出/匯入功能
2. **自動同步**：在所有裝置設定相同的 LINE User ID

### Q: 能否只匯入部分持股？

A: 
1. 匯出完整資料
2. 使用文字編輯器開啟 JSON
3. 刪除不需要的持股
4. 儲存並匯入修改後的檔案

## API 參考

### 匯出 API

```http
GET /api/portfolio/export
Headers:
  X-User-ID: <user_id>

Response:
{
  "stocks": [...],
  "count": 12
}
```

### 匯入 API

```http
POST /api/portfolio/import
Headers:
  X-User-ID: <user_id>
  Content-Type: application/json

Body:
{
  "stocks": [...],
  "clear_existing": true
}

Response:
{
  "success": 10,
  "failed": 2
}
```
